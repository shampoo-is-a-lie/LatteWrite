node_modules/
dist/
out/
*.AppImage
.DS_Store
LW_DATA/
.claude/settings.local.json
# Never in a public repo: working notes, prompt history, unshipped plans.
.claude/memory-seed/
# The macOS fork's checkout, kept here only until the Mac switches to this repo.
LatteWriteMac/
# Bundled Whisper model + ORT wasm — too big for git (decoder > 100MB).
# Regenerated by scripts/fetch-whisper.sh (runs automatically on `npm run dist`).
resources/whisper/
